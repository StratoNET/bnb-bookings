{{template "base" .}}

{{define "content"}}
<div class="container">
  <div class="row">
    <div class="col">
      <h1>About</h1>
      <p>
        {{if ne (index .StringMap "remote_IP") ""}}
          Remote IP : {{index .StringMap "remote_IP"}}
        {{else}}
          Remote IP is currently unknown. Please visit <a href="/">Home</a> page to retrieve it.
        {{end}}
      </p>
    </div>
  </div>
</div>
{{end}}
